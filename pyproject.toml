[project]
name = "cfs-2017"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "iconlit", email = "akpaesudavid@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "pyarrow>=25.0.1",
]

[project.scripts]
cfs-2017 = "cfs_2017:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
